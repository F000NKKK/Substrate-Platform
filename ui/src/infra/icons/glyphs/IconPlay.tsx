import { createIcon } from "../Icon";

/** Solid-filled, unlike every other glyph in the set — a deliberate exception for the one "primary action" button (Run), matching how Visual Studio's own Start arrow is always a solid filled triangle regardless of outline-style chrome elsewhere. */
export const IconPlay = createIcon(<path d="M7 5l12 7-12 7z" fill="currentColor" stroke="none" />);
