import "./tokens.css";

/** CSS custom-property names, typed, so consumers never spell a `--sp-*` string by hand. */
export const tokens = {
  accentH: "--sp-accent-h",
  accentS: "--sp-accent-s",
  accentL: "--sp-accent-l",
} as const;
